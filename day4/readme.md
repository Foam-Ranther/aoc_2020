# Approach 
- parse the main input : 
  - parsing the sub input (which is the nested array of all the values) : get all the keys 
- Check whether each key is present on the our valid keys array. 
- if some key is not present then don't count that password